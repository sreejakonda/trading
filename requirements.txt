# Core (required)
pytz>=2023.3
certifi>=2023.7.22   # verified TLS CA bundle (fixes macOS cert errors)

# Optional — Claude second-opinion advisor (skipped if ANTHROPIC_API_KEY unset)
anthropic>=0.39.0

# Optional — Alpaca broker (required for TRADING_MODE=paper or live)
alpaca-py>=0.8.2
